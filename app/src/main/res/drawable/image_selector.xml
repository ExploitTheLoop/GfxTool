<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <stroke
                android:width="2dp"
                android:color="#FFFFFF"/> <!-- Set white color for the border -->
            <solid android:color="#FF9800"/> <!-- Change to desired pressed color -->
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="2dp"
                android:color="#FAFAFA"/> <!-- Transparent border -->
            <solid android:color="@android:color/transparent"/>
        </shape>
    </item>
</selector>
